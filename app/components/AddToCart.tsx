'use client';

const AddToCart = () => {
  return (
    <div>
      <button
        onClick={() => console.log('Clicked')}
        className="btn btn-soft btn-primary"
      >
        Add to Cart
      </button>
    </div>
  );
};

export default AddToCart;
